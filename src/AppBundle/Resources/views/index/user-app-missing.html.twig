{% extends 'base-error.html.twig' %}

{% block content %}
    <div class="alert alert-danger">
        <h1><i class="fa fa-user"></i></h1>

        <h3 class="font-bold">Application interface non connectée.</h3>

        <div class="error-desc">
            <p>Vous devez vous connecter avec l'interface avant d'utiliser l'intranet.</p>

            <div style="margin-top: 30px;">
                <a class="btn btn-primary btn-block" href="{{ path('login') }}">
                    <i class="fa fa-arrow-circle-o-left"></i> Retour à la connexion
                </a>
            </div>
        </div>
    </div>
{% endblock %}